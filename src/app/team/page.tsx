import TeamSection from "@/domains/teams/components/TeamSection";

export const metadata = {
  title: "Team — SCC RAN Portal",
  description: "Meet the SCC RAN operations team.",
};

export default function TeamPage() {
  return <TeamSection />;
}
